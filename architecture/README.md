# Architecture Assets

- `diagrams/` contains source-controlled diagrams and exported assets.
- `adr/` contains Architecture Decision Records.

Diagrams should be understandable without proprietary software where possible. Mermaid is preferred for diagrams rendered in Markdown; draw.io source files and exported SVG/PNG may be added where visual detail is needed.
